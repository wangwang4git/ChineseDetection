# Tasks: update-ui-design-v2

## Task List

### Phase 1: 样式基础设施

- [x] **T1**: 更新 `styles/variables.scss` 添加新版主题色变量
  - 添加粉紫蓝渐变背景变量
  - 添加主题紫色、金边色变量
  - 添加卡片渐变色变量

### Phase 2: 首页重构

- [x] **T2**: 重构首页 `pages/home/home.vue` UI
  - 更新页面背景为粉紫蓝渐变
  - 添加顶部 banner 图片
  - 更新标题样式（emoji + 文字 + 副标题）
  - 重构年龄段卡片（浅色渐变 + 金边 + emoji 图标）
  - 更新开始检测按钮（粉紫蓝渐变胶囊按钮）
  - 移除底部安全区（由系统 TabBar 处理）

### Phase 3: 个人页重构

- [x] **T3**: 重构个人页 `pages/profile/profile.vue` UI
  - 更新页面背景为粉紫蓝渐变
  - 重构用户信息卡片（粉紫渐变 + 白边 + 头像样式）
  - 重构统计卡片（三色独立渐变：绿/蓝/紫）
  - 更新历史记录标题样式
  - 重构历史记录卡片（浅黄渐变 + 金边）
  - 移除底部安全区（由系统 TabBar 处理）

### Phase 4: 验证

- [x] **T4**: 验证 UI 实现
  - H5 模式下验证首页、个人页显示效果
  - 确认与 Figma 设计稿一致性
  - 验证页面交互功能正常

## Dependencies

```
T1 (样式变量) → T2 (首页) → T4 (验证)
T1 (样式变量) → T3 (个人页) → T4 (验证)
```

T2 和 T3 可并行执行。

## Validation Criteria

- 首页背景为 135° 粉紫蓝渐变
- 年龄段卡片显示 emoji 图标、年龄、目标认字量
- 开始检测按钮为圆角胶囊形状
- 个人页用户卡片显示头像、昵称、账号
- 统计卡片分别显示检测次数、最高记录、平均认字
- 历史记录卡片显示时间、认字量、需加强字数
