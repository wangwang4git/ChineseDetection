# Design: update-ui-design-v2

## Overview

本设计文档描述首页和个人页的 UI 重构方案，基于 Figma 2.0 设计稿实现全新的粉紫蓝渐变视觉风格。

## Visual Design System

### Color Palette

```scss
// 主背景渐变
$bg-gradient: linear-gradient(135deg, #FCCEE8 0%, #E9D4FF 50%, #BEDBFF 100%);

// 主题色
$primary-purple: #9810FA;      // 数字高亮
$primary-purple-dark: #6E11B0; // 标题文字
$accent-gold: #FFDF20;         // 卡片边框

// 用户卡片渐变
$user-card-gradient: linear-gradient(90deg, #FDA5D5 0%, #DAB2FF 50%, #8EC5FF 100%);
$user-card-border: 3.66px solid white;

// 统计卡片渐变
$stat-green: linear-gradient(135deg, #B9F8CF 0%, #7BF1A8 100%);  // 检测次数
$stat-blue: linear-gradient(135deg, #BEDBFF 0%, #8EC5FF 100%);   // 最高记录
$stat-purple: linear-gradient(135deg, #E9D4FF 0%, #DAB2FF 100%); // 平均认字

// 年龄卡片渐变
$age-card-1: linear-gradient(135deg, #FFE5E5 0%, white 100%);  // 4-5岁
$age-card-2: linear-gradient(135deg, #E5F5FF 0%, white 100%);  // 5-6岁
$age-card-3: linear-gradient(135deg, #FFF5E5 0%, white 100%);  // 幼小衔接
$age-card-4: linear-gradient(135deg, #F0FFE5 0%, white 100%);  // 1-2年级
$age-card-5: linear-gradient(135deg, #FFE5F5 0%, white 100%);  // 3-4年级

// 历史记录卡片
$history-card: linear-gradient(135deg, #FFF5E5 0%, white 100%);

// 按钮渐变
$btn-primary: linear-gradient(90deg, #FB64B6 0%, #C27AFF 50%, #51A2FF 100%);
```

### Typography

```scss
// 标题
$title-size: 30px;
$title-weight: 500;
$title-color: #0A0A0A;

// 副标题
$subtitle-size: 16px;
$subtitle-color: #6E11B0;

// 卡片标题
$card-title-size: 18px;
$card-title-weight: 500;
$card-title-color: #1E2939;

// 数字高亮
$number-size: 20px;
$number-color: #9810FA;

// 描述文字
$desc-size: 14px;
$desc-color: #4A5565;

// 小字
$small-size: 12px;
$small-color: #6A7282;
```

## Component Architecture

### 首页 (home.vue)

```
┌─────────────────────────────────────┐
│         Safe Area Top               │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │      Banner Image           │    │
│  │   (360.85 x 128 rpx)        │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│       🎓 汉字认字量检测 📚          │
│     一起来测测认识多少字吧！         │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ 🌱 4-5岁           50-100   │    │
│  │    目标认字量        个汉字  │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🌿 5-6岁          200-300   │    │
│  │    目标认字量        个汉字  │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🌺 幼小衔接        300-500   │    │
│  │    目标认字量        个汉字  │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🌻 1～2年级       1600左右   │    │
│  │    目标认字量        个汉字  │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🌳 3～4年级       2500左右   │    │
│  │    目标认字量        个汉字  │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│     ╭──────────────────────╮        │
│     │ 🚀 开始检测吧！ 🎉   │        │
│     ╰──────────────────────╯        │
└─────────────────────────────────────┘
```

### 个人页 (profile.vue)

```
┌─────────────────────────────────────┐
│         Safe Area Top               │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │  头像  王澈小朋友             │    │
│  │      账号：user_001          │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  ┌─────┐  ┌─────┐  ┌─────┐         │
│  │ 📊  │  │ 🏆  │  │ 📈  │         │
│  │  1  │  │2400 │  │2400 │         │
│  │次检测│  │最高 │  │平均 │         │
│  └─────┘  └─────┘  └─────┘         │
├─────────────────────────────────────┤
│  📚 历史检测记录                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ 2026/1/6 15:56:49           │    │
│  │ 📊 认字量：2400          👉 │    │
│  │ 需加强：5 个汉字             │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

## Implementation Details

### 首页年龄卡片数据结构

```javascript
const ageReferences = [
  {
    emoji: '🌱',
    age: '4-5岁',
    description: '目标认字量',
    range: '50-100',
    unit: '个汉字',
    gradient: 'linear-gradient(135deg, #FFE5E5 0%, white 100%)'
  },
  {
    emoji: '🌿',
    age: '5-6岁',
    description: '目标认字量',
    range: '200-300',
    unit: '个汉字',
    gradient: 'linear-gradient(135deg, #E5F5FF 0%, white 100%)'
  },
  // ...
]
```

### 个人页统计卡片数据结构

```javascript
const statCards = [
  {
    emoji: '📊',
    value: statistics.testCount,
    label: '次检测',
    gradient: 'linear-gradient(135deg, #B9F8CF 0%, #7BF1A8 100%)',
    borderColor: '#05DF72'
  },
  {
    emoji: '🏆',
    value: statistics.maxScore,
    label: '最高记录',
    gradient: 'linear-gradient(135deg, #BEDBFF 0%, #8EC5FF 100%)',
    borderColor: '#51A2FF'
  },
  {
    emoji: '📈',
    value: statistics.avgScore,
    label: '平均认字',
    gradient: 'linear-gradient(135deg, #E9D4FF 0%, #DAB2FF 100%)',
    borderColor: '#C27AFF'
  }
]
```

## Responsive Considerations

- 所有尺寸使用 `rpx` 单位，基于 750rpx 设计稿宽度
- Figma 设计稿宽度 393px 对应 750rpx
- 换算公式: `rpx = px * (750 / 393) ≈ px * 1.91`

## Migration Strategy

1. 保留现有业务逻辑和数据绑定
2. 仅更新模板结构和样式
3. 使用 CSS 变量便于后续主题切换
